import React from 'react';
import RegisterWidget from '../../components/widgets/Auth/RegisterWidget';

const Register = () => {
    return (
        <RegisterWidget />
    );
};

export default Register;